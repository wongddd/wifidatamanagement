server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://mysql:3306/hotel_wifi?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: hotelwifi
    password: HotelWifi@123
  data:
    redis:
      host: redis
      port: 6379

logging:
  level:
    com.alenwifidata: INFO
